#include<stdio.h>
int main()
{
    char str[100];
    int c=0,i,a,b,k=0;
    scanf("%[^\n]s",str);
    scanf("%d",&a);
    scanf("%d",&b);
    for(i=0;str[i]!='\0';i++)
    {
        if(i>=a and i<=b)
        {
            printf("%c",str[i]);
        }
    }
}
