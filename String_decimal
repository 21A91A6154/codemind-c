#include<stdio.h>
int main()
{
    char str[100];
    int c=0,i,n,k;
    scanf("%d",&n);
    for(k=0;k<n;k++)
    {
        int c=0;
    scanf("%s",str);
    for(i=0;str[i]!='\0';i++)
    {
        //printf("%c",str[i]);
        if(str[i]=='0' || str[i]=='1' || str[i]=='2' || str[i]=='3' || str[i]=='4' || str[i]=='5' || str[i]=='6' || str[i]=='7' || str[i]=='8' || str[i]=='9')
        {
            continue;
        }
        else
        {
            c+=1;
            break;
        }
    }
    if(c>0)
    {
        printf("False");
    }
    else
    {
        printf("True");
    }
    printf("\n");
    }
    
}
